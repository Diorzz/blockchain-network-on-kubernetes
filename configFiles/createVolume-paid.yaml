kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shared-pvc
  labels:
    app: blockchain
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: "ceph-storageclass-c3-cc"
  resources:
    requests:
      storage: 2Gi
