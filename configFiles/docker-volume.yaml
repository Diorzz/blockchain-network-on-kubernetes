# ---
# kind: PersistentVolume
# apiVersion: v1
# metadata:
#   name: docker-pv
#   labels:
#     type: local
#     name: dockervolume
# spec:
#   capacity:
#     storage: 10Gi
#   accessModes:
#     - ReadWriteMany
#   hostPath:
#     path: "/tmp-docker-pv"

---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: docker-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-storageclass-c3-cc
  resources:
    requests:
      storage: 8Gi
  selector:
    matchLabels:
      name: dockervolume